= form_with(model: @form, url: login_path, local: true) do |f|
  - if @failure
    #error_explanation
      %h2 ログインできません。

  .field
    = f.label "メールアドレス"
    = f.text_field :email

  .field
    = f.label "パスワード"
    = f.password_field :password

  .actions
    = form.submit "ログイン"

= link_to "新規登録", signup_path
